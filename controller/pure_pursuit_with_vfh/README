
This ROS package is auto-generated from Simulink model inside simulink_model folder using Matlab Code Generator.


Model Parameters and their initial value at compilation time:
- see the header file 'ModelName.h' for available parameters and their default values in the generated model. 'ModelName' is the name of the Simulink model (without extension .slx).

Setting model parameters at runtime:

- add custom C code inside the model initialize() function generated by Code Generator in this way:
1. Open the Simulink model
2. Go to Simulation -> Model Configuration Parameters -> Code Generation -> Custom Code
3. Put the custom code in the 'Initialize function', e.g.

double desired_lin_vel;

// SLROSNodePtr is a global ROS Node Handle used to access ROS Parameter Server
SLROSNodePtr->param<double>("desired_lin_vel", desired_lin_vel, 0.3);
ROS_WARN_STREAM("Desired Lin Vel = " << desired_lin_vel);

/*
this->PathFollowingWithObstacleAvoi_P is a structure that holds tunable model parameters. Check the generated code to find out its elements and set the desired ones.
*/
this->PathFollowingWithObstacleAvoi_P.PurePursuit_DesiredLinearVeloci = desired_lin_vel;

